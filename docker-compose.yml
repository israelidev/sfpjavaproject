services:

  frontend:
      build:
        context: .
        dockerfile: Dockerfile
      ports:
        - "8080:80"
      depends_on:
        - api

  api:
      build:
        context: .
        dockerfile: Dockerfile.api
      ports:
        - "8081:8081"





  